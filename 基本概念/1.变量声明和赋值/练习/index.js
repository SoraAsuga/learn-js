/**
 * 1. 声明一个变量 glj
 * 2. 给 glj 赋值 "hello world"
 * 3. 声明一个变量 constant 并赋值 10，并且后续不会再对 constant 进行修改
 * 4. 在控制台分别输出 glj 和 constant
 */

 let glj;
 glj = "hello world";
 const constant = 10;
 console.log(glj);
 console.log(constant);